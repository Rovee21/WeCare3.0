SECRET_KEY=replace-with-a-long-random-string
DEBUG=True
DATABASE_URL=postgres://wecare:wecare_local@db:5432/wecare
ALLOWED_HOSTS=*
CORS_ALLOW_ALL_ORIGINS=True

# AWS (leave blank for local dev — only needed for S3/Transcribe)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=
AWS_TRANSCRIBE_OUTPUT_BUCKET=

# Email (console backend used in local.py — set for production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=wecaremason@gmail.com
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
