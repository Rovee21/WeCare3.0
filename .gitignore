# React Native / Expo
**/node_modules
mobile/.expo
mobile/dist
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables — never commit these
.env
.env.local
.env.production

# Firebase credentials — add via EAS secrets instead
google-services.json
GoogleService-Info.plist

# iOS / Android signing
*.keystore
*.jks
credentials.json
*.pem
*.p8
*.p12

# macOS
.DS_Store

# Claude Code project instructions (internal, not for contributors)
CLAUDE.md

# Local media files (dev only — production uses S3)
backend/media/

# CONTENT_SCHEDULING_HANDOFF.md
# SEQUENTIAL_UNLOCK_ADDENDUM.md
wecare3-notifications-0e536142e2b6.json

Z_Instructions_Claude_Code/
wecare-backend-deploy*.zip